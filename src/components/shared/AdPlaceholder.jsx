// AdPlaceholder is intentionally disabled.
// All former call sites remain wired in but render nothing.
// Re-enable individual placements with PostResultBanner once real AdSense slots are set.
export default function AdPlaceholder() {
  return null;
}
